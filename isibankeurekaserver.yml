server:
  port: 8070

isibankeurekaserver:
  instance:
    hostname: localhost
  client:
    fetchRegistry: false
    registerWithEureka: false
    serviceUrl:
      defaultZone: http://${isibankeurekaserver.instance.hostname}:${server.port}/eureka/
    
